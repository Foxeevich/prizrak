<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Уведомления о сообщениях (Android 13+ требует runtime-разрешение) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Установка обновлений (APK из нашего канала обновлений) -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Звонки: микрофон (аудио) и камера (видео) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Сохранение файлов в «Загрузки» на старых Android (до API 29; новее — MediaStore без разрешений) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <!-- Призрак-VPN: системный tun («Замаскироваться») работает как foreground-служба -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Авто-подъём VPN после перезагрузки -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Prizrak общается с self-hosted серверами по HTTP на нестандартных портах
         (8801 и т.п.), пока на них не настроен TLS. Android 9+ по умолчанию блокирует
         cleartext в release («Network request failed») — разрешаем явно.
         Сам трафик Prizrak при этом E2E-зашифрован поверх HTTP. -->
    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:usesCleartextTraffic="true"
      android:theme="@style/AppTheme">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
      <!-- Призрак-VPN: системный туннель. BIND_VPN_SERVICE обязателен для VpnService. -->
      <service
        android:name="com.prizrakmobile.vpn.PrizrakVpnService"
        android:permission="android.permission.BIND_VPN_SERVICE"
        android:foregroundServiceType="specialUse"
        android:exported="false">
        <intent-filter>
          <action android:name="android.net.VpnService" />
        </intent-filter>
        <property
          android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
          android:value="Prizrak stealth VPN tunnel" />
      </service>
      <!-- Авто-подъём VPN после ребута -->
      <receiver
        android:name="com.prizrakmobile.vpn.BootReceiver"
        android:enabled="true"
        android:exported="true">
        <intent-filter>
          <action android:name="android.intent.action.BOOT_COMPLETED" />
          <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
          <action android:name="android.intent.action.QUICKBOOT_POWERON" />
        </intent-filter>
      </receiver>
    </application>
</manifest>
